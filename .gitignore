# ─── Generated code ───
# proto codegen (gen/) IS committed as the cross-service source of truth and
# drift-checked in CI (buf generate must produce no diff). Do not re-ignore it.

# ─── Go ───
services/market-district/bin/
*.exe

# ─── Rust ───
services/fortress/target/

# ─── Node/TypeScript ───
node_modules/
dist/
*.tsbuildinfo

# ─── Python ───
__pycache__/
*.pyc
.venv/
*.egg-info/
.mypy_cache/
.ruff_cache/

# ─── IDE / local agent tooling ───
.vscode/
.idea/
*.swp
*.swo

# Claude Code / MCP local machine configuration. These can contain broad
# permissions, absolute local paths, and API keys; keep them out of git.
.claude/settings.local.json
.mcp.json

# ─── OS ───
.DS_Store
Thumbs.db

# ─── Docker ───
*.log

# ─── Env ───
.env
.env.local

# secrets (rotated; untracked 2026-05-30)
.env
.env.*
!.env.example
.dev.vars
*.dev.vars
*.p8
*.pem
*.key

# local test artifacts
services/town-core/test_town.db
