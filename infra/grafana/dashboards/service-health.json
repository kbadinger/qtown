{
  "__inputs": [
    {
      "name": "DS_PROMETHEUS",
      "label": "Prometheus",
      "description": "",
      "type": "datasource",
      "pluginId": "prometheus",
      "pluginName": "Prometheus"
    },
    {
      "name": "DS_JAEGER",
      "label": "Jaeger",
      "description": "",
      "type": "datasource",
      "pluginId": "jaeger",
      "pluginName": "Jaeger"
    }
  ],
  "__requires": [
    {"type": "grafana", "id": "grafana", "name": "Grafana", "version": "10.0.0"},
    {"type": "datasource", "id": "prometheus", "name": "Prometheus", "version": "1.0.0"},
    {"type": "panel", "id": "timeseries", "name": "Time series", "version": ""},
    {"type": "panel", "id": "gauge", "name": "Gauge", "version": ""},
    {"type": "panel", "id": "stat", "name": "Stat", "version": ""},
    {"type": "panel", "id": "row", "name": "Row", "version": ""}
  ],
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {"type": "grafana", "uid": "-- Grafana --"},
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "description": "Per-service request rate, error rate, latency, and Kafka consumer lag",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 0},
      "id": 100,
      "title": "town-core",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {
        "defaults": {
          "color": {"mode": "palette-classic"},
          "custom": {"lineWidth": 2, "fillOpacity": 10},
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 1},
      "id": 1,
      "options": {"tooltip": {"mode": "multi"}, "legend": {"displayMode": "list"}},
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(kafka_produce_total{service=\"town-core\"}[5m])",
          "legendFormat": "kafka produce/s",
          "refId": "A"
        }
      ],
      "title": "town-core — Kafka Produce Rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {
        "defaults": {"color": {"mode": "palette-classic"}, "unit": "s", "custom": {"lineWidth": 2}},
        "overrides": []
      },
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 1},
      "id": 2,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.50, sum by(le) (rate(tick_duration_seconds_bucket{service=\"town-core\"}[5m])))",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.95, sum by(le) (rate(tick_duration_seconds_bucket{service=\"town-core\"}[5m])))",
          "legendFormat": "p95",
          "refId": "B"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.99, sum by(le) (rate(tick_duration_seconds_bucket{service=\"town-core\"}[5m])))",
          "legendFormat": "p99",
          "refId": "C"
        }
      ],
      "title": "town-core — Tick Duration (p50/p95/p99)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {
        "defaults": {"color": {"mode": "thresholds"}, "unit": "short",
          "thresholds": {"mode": "absolute", "steps": [
            {"color": "green", "value": null},
            {"color": "yellow", "value": 1},
            {"color": "red", "value": 10}
          ]}
        }
      },
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 1},
      "id": 3,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(error_total{service=\"town-core\"}[5m])",
          "legendFormat": "errors/s",
          "refId": "A"
        }
      ],
      "title": "town-core — Error Rate",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 9},
      "id": 101,
      "title": "market-district",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "reqps", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 10},
      "id": 10,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(grpc_request_total{service=\"market-district\"}[5m])",
          "legendFormat": "{{method}} ({{status}})",
          "refId": "A"
        }
      ],
      "title": "market-district — gRPC Request Rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "s", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 10},
      "id": 11,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.50, sum by(le) (rate(order_match_duration_seconds_bucket{service=\"market-district\"}[5m])))",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.95, sum by(le) (rate(order_match_duration_seconds_bucket{service=\"market-district\"}[5m])))",
          "legendFormat": "p95",
          "refId": "B"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.99, sum by(le) (rate(order_match_duration_seconds_bucket{service=\"market-district\"}[5m])))",
          "legendFormat": "p99",
          "refId": "C"
        }
      ],
      "title": "market-district — Order Match Latency (p50/p95/p99)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 10},
      "id": 12,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(error_total{service=\"market-district\"}[5m])",
          "legendFormat": "errors/s",
          "refId": "A"
        }
      ],
      "title": "market-district — Error Rate",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 18},
      "id": 102,
      "title": "fortress",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "reqps", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 19},
      "id": 20,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(validations_total{service=\"fortress\"}[5m])",
          "legendFormat": "validations/s ({{result}})",
          "refId": "A"
        }
      ],
      "title": "fortress — Validation Rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "s", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 19},
      "id": 21,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.50, sum by(le) (rate(validation_duration_seconds_bucket{service=\"fortress\"}[5m])))",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.95, sum by(le) (rate(validation_duration_seconds_bucket{service=\"fortress\"}[5m])))",
          "legendFormat": "p95",
          "refId": "B"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.99, sum by(le) (rate(validation_duration_seconds_bucket{service=\"fortress\"}[5m])))",
          "legendFormat": "p99",
          "refId": "C"
        }
      ],
      "title": "fortress — Validation Latency (p50/p95/p99)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 19},
      "id": 22,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(rejections_total{service=\"fortress\"}[5m])",
          "legendFormat": "rejections/s",
          "refId": "A"
        }
      ],
      "title": "fortress — Rejection Rate",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 27},
      "id": 103,
      "title": "academy",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "s", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 28},
      "id": 30,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.50, sum by(le, model) (rate(inference_duration_seconds_bucket{service=\"academy\"}[5m])))",
          "legendFormat": "{{model}} p50",
          "refId": "A"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.95, sum by(le, model) (rate(inference_duration_seconds_bucket{service=\"academy\"}[5m])))",
          "legendFormat": "{{model}} p95",
          "refId": "B"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.99, sum by(le, model) (rate(inference_duration_seconds_bucket{service=\"academy\"}[5m])))",
          "legendFormat": "{{model}} p99",
          "refId": "C"
        }
      ],
      "title": "academy — Inference Latency by Model (p50/p95/p99)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "s", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 28},
      "id": 31,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.95, sum by(le) (rate(rag_query_duration_seconds_bucket{service=\"academy\"}[5m])))",
          "legendFormat": "RAG p95",
          "refId": "A"
        }
      ],
      "title": "academy — RAG Query Latency",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 28},
      "id": 32,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(tokens_total{service=\"academy\"}[5m])",
          "legendFormat": "{{model}} {{direction}}",
          "refId": "A"
        }
      ],
      "title": "academy — Tokens/s by Model",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 36},
      "id": 104,
      "title": "tavern",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 37},
      "id": 40,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "ws_connections_total{service=\"tavern\"}",
          "legendFormat": "active connections",
          "refId": "A"
        }
      ],
      "title": "tavern — Active WebSocket Connections",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "msgps", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 37},
      "id": 41,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(ws_messages_total{service=\"tavern\"}[5m])",
          "legendFormat": "{{channel}} ({{direction}})",
          "refId": "A"
        }
      ],
      "title": "tavern — WS Message Rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "ops", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 37},
      "id": 42,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "rate(redis_ops_total{service=\"tavern\"}[5m])",
          "legendFormat": "{{operation}} ({{status}})",
          "refId": "A"
        }
      ],
      "title": "tavern — Redis Ops/s",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 45},
      "id": 105,
      "title": "Kafka Consumer Lag",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {
        "defaults": {
          "color": {"mode": "thresholds"},
          "unit": "short",
          "thresholds": {"mode": "absolute", "steps": [
            {"color": "green", "value": null},
            {"color": "yellow", "value": 1000},
            {"color": "red", "value": 10000}
          ]}
        },
        "overrides": []
      },
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 46},
      "id": 50,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "sum by(topic, group) (kafka_consumer_group_lag{job=\"kafka\"})",
          "legendFormat": "{{group}} / {{topic}}",
          "refId": "A"
        }
      ],
      "title": "Kafka Consumer Group Lag",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": {"h": 1, "w": 24, "x": 0, "y": 54},
      "id": 106,
      "title": "NPC Travel Traces",
      "type": "row"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "s", "custom": {"lineWidth": 2}}, "overrides": []},
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 55},
      "id": 60,
      "targets": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "expr": "histogram_quantile(0.95, sum by(le) (rate(tick_duration_seconds_bucket{service=\"town-core\"}[5m])))",
          "legendFormat": "tick p95 (proxy for NPC travel)",
          "refId": "A"
        }
      ],
      "title": "NPC Travel — Tick p95 Duration",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["qtown", "services", "health"],
  "templating": {
    "list": [
      {
        "current": {},
        "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
        "definition": "label_values(service)",
        "hide": 0,
        "includeAll": true,
        "multi": true,
        "name": "service",
        "options": [],
        "query": "label_values(service)",
        "refresh": 2,
        "sort": 1,
        "type": "query"
      }
    ]
  },
  "time": {"from": "now-1h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "Qtown — Service Health",
  "uid": "qtown-service-health",
  "version": 1
}
